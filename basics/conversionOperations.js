// let score = 'null';

// let stringToNumber = Number(score);

// console.log(stringToNumber);
// console.log(typeof stringToNumber);

/*
Notes:-

if score is 33 type => number
if score is '33' type => number but value is NaN
if score is 'test' type => number but value is NaN
if score is null type => number but value 0
*/

// let isLoggedIn = 1;

// let boolean = Boolean(isLoggedIn);

// console.log(boolean);

/*
Notes:- 

if isLoggedIn is 1 value => true
if isLoggedIn is 0 value => false

if isLoggedIn is "" value => false
if isLoggedIn "anything" value => true
*/

// ***************************************** Operations *****************************************

console.log(2 + 2); // OUtput = 4
console.log(2 + "2"); // OUtput = 22 number + string
console.log("2" + "2"); // OUtput = 22 string + string
console.log(2 + 2 + "2"); // OUtput = 42 first datatype is decide to convert other datatype.
